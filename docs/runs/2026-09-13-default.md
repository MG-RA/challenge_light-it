# Default run — 2026-09-13

Final submission review, against the working tree. Command: `RUN_MUTATING=0 npm test` (PowerShell environment assignment), four workers, zero retries. Started **2026-09-13 13:50:57 UTC**; duration **14.0 seconds**. Windows, Node 26.8.2, Playwright 1.63.0, Chromium. Exit code **0**. The sandbox initially blocked worker launch; the successful run used an approved execution outside that sandbox.

| Project | Cases | Normal passes | Known failures reproduced | Skipped | Unexpected |
|---|---:|---:|---:|---:|---:|
| setup | 1 | 1 | 0 | 0 | 0 |
| api | 35 | 29 | 6 | 0 | 0 |
| db | 2 | 2 | 0 | 0 | 0 |
| ui | 11 | 10 | 1 | 0 | 0 |
| **Total** | **49** | **42** | **7** | **0** | **0** |

No flaky results or unexpected passes. The seven recorded errors were reviewed: F-01 missing doctor-list fields (one case); F-13 banner size of 6,442,770 bytes against 512,000 bytes (one); F-14 public cache headers (two); F-15 UTC-midnight appointment timestamps (two); F-20 unsigned-token edge response of 403 instead of 401 (one). Playwright's headline is `49 passed`, which includes these expected failures.

## Refinements and checks

After the live run, the owned-write helper was refined to remember create markers before sending the request and reconcile them during teardown. This recovers rows after a lost response or failed initial DB lookup. Paid-record cancellation now requires observed DB state before being reported as cancelled residue.

Five offline scenarios against the compiled helper passed: normal cleanup, lost create response, initial DB lookup failure, verified paid-record cancellation, and HTTP 200 cancellation without a stored change (correctly fails cleanup). Each also checked refusal to delete an unowned ID. These were local mocked checks, not live write evidence. Type checking and lint passed after the code change.

The default live run preceded this write-only helper change. No live writes were performed during this review; [2026-09-12-writes.md](2026-09-12-writes.md) remains the latest write evidence. Process interruption can still leave rows; markers are in memory, and the observation window cannot rule out later writes. Per-process caps reset when Playwright replaces a failed worker.

No hosted CI run, second controlled account, accessibility scan, mobile or other-browser execution was performed. Raw reports remain local and unsanitized. This sanitized record is suitable for the repository.
