# Write run — 2026-09-12

First live run of the `@mutating` cases in their current form: the `owned` fixture in
`tests/api/writes.ts` and the `tests/api/dbState.ts` helpers. It supersedes
[STATE_EXECUTION.md](../STATE_EXECUTION.md) for current results.

Command: `$env:RUN_MUTATING='1'; npm run test:writes`, which forces **1 worker, 0 retries** and sequential
order. Started **2026-09-12 22:08:26 UTC** (19:08:26 America/Montevideo), 50 s after the
[default run](2026-09-12-default.md). Duration **118.0 s**. Same environment and tree as the default run.
Exit code **1**, because real failures stay visible.

| Category | Result |
|---|---:|
| Cases selected | 14: setup plus 13 write cases |
| Normal passes | 5, including setup |
| Failed scenarios | 8 |
| Skipped | 1 |
| Expected-failure markers | 0 |
| Cleanup failures / residue annotations | 0 / 0 |
| Booking submissions / persisted appointments | 15 / 14 (the missing-doctor create stored nothing) |
| Payment submissions / persisted payments | 1 / 0 |
| New notifications for the account | 0 |

Submission counts come from the case structure, not from a cap counter. Each failure restarts the Playwright
worker, which resets the in-module caps (see review item #4).

## Per-scenario results

| Case | Result | Evidence |
|---|---|---|
| Valid create, detail agrees | Pass | Exactly one owned row; API detail matched it |
| Valid reschedule | Pass | New slot stored; identity and ownership kept |
| Cancel with control | **Fail, F-16** | 200, but appointment 1126 stayed `active` through the observation window. The control assertion was not reached. |
| Delete | Pass | Row gone; detail returned 404 |
| Missing doctor | Pass | 400; no marked row |
| Yesterday | **Fail, F-12** | 201; 1129 stored `2026-09-11` |
| Year 0123 | **Fail, F-12** | 201; 1130 stored `0123-11-23` |
| Invalid clock | **Fail, F-02** | 201; 1131 stored `25:99` |
| Inactive doctor | **Fail, F-04** | 201; 1132 stored against doctor 7 (`not is_active`) |
| Sequential duplicate | **Fail, F-03** | Second create 201; 1134 stored for the same doctor, date and slot as the first booking |
| Invalid reschedule | **Fail, F-17** | 200; 1135 changed from `2026-09-18 / 09:00` to `2026-09-11 / 25:99` |
| Notification read | Skipped | No new unread notification could be attributed to this run |
| Payment: valid, then zero, negative, duplicate | **Fail, F-18** | Valid cash payment returned 200, but no payment row appeared for appointment 1137. Zero, negative and duplicate steps were not executed. |

All eight failures reproduce the 2026-09-11 signatures. No write defect appears fixed.

The date cases ran at 22:09 UTC, outside the 00:00–03:00 UTC window where `dateAfter(-1)` equals the
Montevideo calendar day (review item #6).

## Cleanup and residue check

A read-only snapshot of the account was taken just before the default run and again at **22:10:30 UTC**,
after the write run. The snapshot covered:

- marker appointments (`notes like 'qa-suite %'`)
- the account's appointment ids
- linked payments
- notification ids
- the global maximum appointment and payment ids

| Snapshot | Marker rows | Own appointments | Own payments | Own notifications | Max payment id |
|---|---:|---:|---:|---:|---:|
| Before (22:07:25 UTC) | 0 | 9 | 1 | 2 | 77 |
| After (22:10:30 UTC) | 0 | 9 | 1 | 2 | 77 |

The snapshots are identical. Every created appointment was deleted, no payment persisted, and no residue
remains.

Local reports are unsanitized and not committed, because they contain account data and run markers.
