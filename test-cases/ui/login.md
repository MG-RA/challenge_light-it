# Login page and access control (UI)

Spec: [tests/ui/login.spec.ts](../../tests/ui/login.spec.ts) · Project: `ui` (Desktop Chrome, depends on `setup`)

**Starting state:** every case in this file overrides `storageState` to empty (`cookies: []`, `origins: []`). The browser starts **logged out**, without the token saved by setup.

**Page object:** [LoginPage](../../src/pages/LoginPage.ts). It exposes the email input (`#email`), the password input (`#password`), the `Sign In` button, the rejection text `Invalid email or password`, and the throttling text (`too many`, `try again in` or `wait … seconds`). The validation cases come from a table of scenario, email, password and the field expected to be invalid.

---

## TC-UI-LOGIN-001: Valid credentials land on the dashboard

| Field | Value |
|---|---|
| Automated test | [login.spec.ts:11](../../tests/ui/login.spec.ts:11) › `Login page › valid credentials land on the dashboard` |
| Project / tag | ui / — |
| Type | Functional, E2E, smoke |
| Priority | P1 |
| Finding | — |
| Last recorded | Pass (2026-09-13) |

**Preconditions:** the browser is logged out, valid credentials are configured, and the login rate limit is not exhausted. This case adds one login.

| # | Action | Expected result |
|---|---|---|
| 1 | Open `/login`. | The login page loads. |
| 2 | Fill **Email** and **Password** with the configured credentials, then click **Sign In**. | — |
| 3 | Observe the URL. | The URL ends with `/dashboard`. |
| 4 | Observe the dashboard. | The greeting heading (the `h1` in `main`) is visible. |

---

## TC-UI-LOGIN-002: Invalid password keeps the user on `/login`

| Field | Value |
|---|---|
| Automated test | [login.spec.ts:23](../../tests/ui/login.spec.ts:23) › `Login page › invalid password keeps the user on /login` |
| Project / tag | ui / — |
| Type | Security, negative, UX feedback |
| Priority | P1 |
| Finding | — |
| Last recorded | Pass (2026-09-13) |

**Preconditions:** the browser is logged out. This case adds one failed login attempt.

**Test data:** the configured email with the password `definitely-wrong`.

| # | Action | Expected result |
|---|---|---|
| 1 | Open `/login`. | The login page loads. |
| 2 | Fill the configured email and `definitely-wrong`, click **Sign In**, and wait for the `POST /api/auth/login` response. | The network response status is **401**. |
| 3 | Observe the page feedback. | Text containing `Invalid email or password` is visible. |
| 4 | Observe the submit button. | **Sign In** is enabled again, so the user can retry. |
| 5 | Observe the URL. | The URL still ends with `/login`. |

---

## TC-UI-ACCESS-001: Unauthenticated visit to `/dashboard` redirects to `/login`

| Field | Value |
|---|---|
| Automated test | [login.spec.ts:34](../../tests/ui/login.spec.ts:34) › `Access control › unauthenticated visit to /dashboard redirects to /login` |
| Project / tag | ui / — |
| Type | Security, route guard |
| Priority | P0 |
| Finding | — |
| Last recorded | Pass (2026-09-13) |

**Preconditions:** the browser is logged out (empty storage state). No login happens in this case.

| # | Action | Expected result |
|---|---|---|
| 1 | Navigate directly to `/dashboard`. | — |
| 2 | Observe the URL. | The browser is redirected, and the URL ends with `/login`. |

---

## TC-UI-LOGIN-003 … 005: Login validates fields before submitting

Generated by the loop at [login.spec.ts:51](../../tests/ui/login.spec.ts:51). Type: functional, client-side validation. Priority P2. Finding: —. Last recorded: **Pass for all 3** (2026-09-13).

**Preconditions:** the browser is logged out. `POST /api/auth/login` is intercepted and counted, so no real login attempt can be sent or lock the account.

| # | Action | Expected result |
|---|---|---|
| 1 | Open `/login` and fill the fields for the case (table below). | — |
| 2 | Click **Sign In**. | — |
| 3 | Inspect the invalid field. | It is focused and `validity.valid` is `false`. |
| 4 | Check the intercepted login requests and the URL. | Zero login requests; the URL still ends with `/login`. |

| Case | Playwright title | Email | Password | Invalid field |
|---|---|---|---|---|
| TC-UI-LOGIN-003 | `login validates missing email before submitting` | empty | `deliberately-invalid` | Email (required) |
| TC-UI-LOGIN-004 | `login validates missing password before submitting` | `qa@example.com` | empty | Password (required) |
| TC-UI-LOGIN-005 | `login validates malformed email before submitting` | `invalid` | `deliberately-invalid` | Email (format) |

---

## TC-UI-LOGIN-006: Login shows actionable feedback for throttling

| Field | Value |
|---|---|
| Automated test | [login.spec.ts:70](../../tests/ui/login.spec.ts:70) › `login shows actionable feedback for throttling` |
| Project / tag | ui / — |
| Type | UX feedback, controlled response |
| Priority | P2 |
| Finding | — |
| Last recorded | Pass (2026-09-13) |

**Preconditions:** the browser is logged out. `POST /api/auth/login` is fulfilled with HTTP **429**, `Retry-After: 60` and `{ error: 'Too many requests. Try again in 60 seconds.' }`.

| # | Action | Expected result |
|---|---|---|
| 1 | Open `/login`, fill dummy credentials `qa@example.com` / `deliberately-invalid`, click **Sign In**. | The intercepted 429 is returned. |
| 2 | Observe the page. | Feedback matching `too many`, `try again in` or `wait … seconds` is visible. |
| 3 | Observe the URL. | It still ends with `/login`. |

**Limits:** the response is simulated, so this checks UI handling only. Actual enforcement is TC-AUTH-RATE-001 in [rate-limit.md](../api/rate-limit.md).
