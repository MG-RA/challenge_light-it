# Booking form (UI)

Specs: [tests/ui/booking.spec.ts](../../tests/ui/booking.spec.ts) and [tests/ui/booking-state.spec.ts](../../tests/ui/booking-state.spec.ts) · Project: `ui` (Desktop Chrome, depends on `setup`)

**Page object:** [BookingPage](../../src/pages/BookingPage.ts). It exposes the Doctor select (`#doctor_id`), the Date input (`#appointment_date`), and the Time Slot options (`#time_slot`), excluding the placeholder "Select a time slot".

---

## TC-UI-BOOK-001: Booking loads the selected doctor's availability into time options without submitting

| Field | Value |
|---|---|
| Automated test | [booking.spec.ts:5](../../tests/ui/booking.spec.ts:5) › `booking loads the selected doctor availability into time options without submitting` |
| Project / tag | ui / — |
| Type | Integration (UI ↔ API), regression |
| Priority | P1 |
| Finding | **F-19** (reported load issue, not reproduced) |
| Last recorded | Pass (2026-09-13). Two doctors' availability GETs returned 200, and the options matched. |

**Preconditions**
- The browser is authenticated.
- At least **two** active doctors exist in the DB, so a change of selection can be observed. Otherwise the case is **skipped**.

**Safety:** every non-GET request to `**/api/**` is **aborted** at the network layer. Even if the app unexpectedly submits, no booking is created, so the case is read-only.

**Test data:** the first two active doctors by `id`, and tomorrow's date in `TEST_TIMEZONE` (default UTC, `YYYY-MM-DD`).

| # | Action | Expected result |
|---|---|---|
| 1 | Read the first two active doctors from the DB. | Two rows are returned. |
| 2 | Install a route guard: allow `GET /api/**`, abort every other method. | — |
| 3 | Open `/appointments/new`. | The booking form loads. |
| 4 | Fill **Date** with tomorrow. | — |
| 5 | For doctor 1: start waiting for `GET /api/doctors/{id}/availability`, then select the doctor's `id` in **Doctor**. | An availability request is sent for **that** doctor's ID. |
| 6 | Inspect the response. | HTTP **200**. The body contains a `time_slots` array, and every slot matches `^(?:[01]\d\|2[0-3]):[0-5]\d$`. |
| 7 | Inspect the **Time Slot** options. | The option texts equal `time_slots` **exactly and in order**, excluding the placeholder. |
| 8 | Repeat steps 5–7 for doctor 2. | A new request is sent for doctor 2, and the options are **replaced** with doctor 2's `time_slots`. |

**Postconditions:** no write requests reached the API.

**This case only:** API-to-UI slot-list parity. Expanded cases below cover failure/recovery, distinct doctor responses, validation and occupied-slot exclusion.

---

## Controlled-response cases (TC-UI-BOOK-002 … 007)

These cases replace the availability response with fixed slots and intercept `POST /api/appointments`, counting submissions and returning 400. No booking can reach the server. Because the responses are simulated, they verify UI behavior only, not backend rules.

### TC-UI-BOOK-002 … 004: Booking validates required fields before submitting

Generated by the loop at [booking.spec.ts:35](../../tests/ui/booking.spec.ts:35). Type: functional, client-side validation. Priority P1. Finding: —. Last recorded: **Pass for all 3** (2026-09-13).

| # | Action | Expected result |
|---|---|---|
| 1 | Open `/appointments/new`; availability returns `['09:00']`. | — |
| 2 | Fill every field except the missing one: doctor = first option, date = 30 days from today (relative, so it stays valid after a minimum-date fix), slot = `09:00`. | — |
| 3 | Click **Book Appointment**. | The missing field is focused and `validity.valid` is `false`. |
| 4 | Check submissions and the URL. | Zero booking submissions; the URL still ends with `/appointments/new`. |

| Case | Playwright title | Missing field |
|---|---|---|
| TC-UI-BOOK-002 | `booking validates missing doctor_id before submitting` | Doctor (no slot can be selected either) |
| TC-UI-BOOK-003 | `booking validates missing appointment_date before submitting` | Date |
| TC-UI-BOOK-004 | `booking validates missing time_slot before submitting` | Time Slot |

### TC-UI-BOOK-005: Switching doctors replaces slots and clears the previous selection

| Field | Value |
|---|---|
| Automated test | [booking.spec.ts:66](../../tests/ui/booking.spec.ts:66) › `switching doctors replaces slots and clears the previous selection` |
| Priority / finding | P1 / — |
| Last recorded | Pass (2026-09-13) |

| # | Action | Expected result |
|---|---|---|
| 1 | Select the first doctor; availability returns `['09:00']`. Select `09:00`. | — |
| 2 | Select the second doctor; availability returns `['14:30']`. | Only `14:30` is offered, and the Time Slot value is reset to empty. |

### TC-UI-BOOK-006: Availability failure shows feedback and recovers

| Field | Value |
|---|---|
| Automated test | [booking.spec.ts:82](../../tests/ui/booking.spec.ts:82) › `availability failure has feedback and recovers after changing doctor` |
| Priority / finding | P1 / F-19 (related report, not reproduced) |
| Last recorded | Pass (2026-09-13) |

| # | Action | Expected result |
|---|---|---|
| 1 | Select the first doctor; availability returns HTTP 500. | Failure feedback (`availability unavailable`, `unable to load` or `failed to … availability`) is visible, and no slots are offered. |
| 2 | Select the second doctor; availability returns `['14:30']`. | Only `14:30` is offered. |

### TC-UI-BOOK-007: Booking rejects a past date in the UI

| Field | Value |
|---|---|
| Automated test | [booking.spec.ts:103](../../tests/ui/booking.spec.ts:103) › `booking rejects a past date in the UI` |
| Priority / basis / finding | P1 / Q (past-date rejection is a proposed rule) / **F-12** |
| Last recorded | **Expected failure, F-12** (2026-09-13): the form was fully populated, then `2000-01-01` stayed valid (no `min` attribute) and focus did not return to Date. The intercepted POST meant nothing reached the server. |

The doctor, date and slot values are asserted **before** the `test.fail` marker, so a broken form or availability load fails normally; only the focus, `rangeUnderflow` and no-submission checks after it are the known defect.

| # | Action | Expected result |
|---|---|---|
| 1 | Select the first doctor, fill Date with `2000-01-01`, select `09:00`. | — |
| 2 | Click **Book Appointment**. | Date is focused and `validity.rangeUnderflow` is `true`. |
| 3 | Check submissions. | Zero booking submissions. |

---

## TC-UI-BOOK-008: A booked doctor, date and slot cannot be selected again

| Field | Value |
|---|---|
| Automated test | [booking-state.spec.ts:4](../../tests/ui/booking-state.spec.ts:4) › `booked doctor date and slot cannot be selected again` |
| Project / tag | ui / **@mutating** |
| Type | Integration (UI ↔ API ↔ DB), business rule |
| Priority / basis | P1 / Q (date-specific vacancy is a proposed rule) |
| Finding | **F-05** |
| Last recorded | **Fail, F-05** (2026-09-13): the booked time stayed present and enabled. The owned appointment was deleted and its absence verified. |

**Preconditions:** `RUN_MUTATING=1`, one worker, zero retries, and a free catalog slot within 30 days (see the shared `@mutating` mechanics in [appointments.md](../api/appointments.md#shared-mechanics)).

| # | Action | Expected result |
|---|---|---|
| 1 | `owned.book()` creates one marked future appointment through the API. | HTTP 201 with exactly one owned row. |
| 2 | Open `/appointments/new` and fill Date with the booked date. | — |
| 3 | Select the booked doctor. | `GET /api/doctors/{id}/availability` returns 200. |
| 4 | Inspect the Time Slot option for the booked time, retrying for up to 7 s. | The option is **absent or disabled**. |

**Cleanup:** the `owned` fixture deletes the appointment and verifies absence, including after a failure. No second booking is made; server-side duplicate rejection is TC-APT-014. Identical schedules across doctors are not a defect on their own.
